https://github.com/termux/termux-packages/issues/22542

--- a/cube/cube.c
+++ b/cube/cube.c
@@ -3915,7 +3915,11 @@ static void demo_check_and_set_wsi_platform(struct demo *demo) {
 #endif
 #if defined(VK_USE_PLATFORM_DISPLAY_KHR)
     if (demo->wsi_platform == WSI_PLATFORM_DISPLAY ||
+#ifdef __ANDROID__
+        demo->wsi_platform == WSI_PLATFORM_DISPLAY) {
+#else
         demo->wsi_platform == WSI_PLATFORM_AUTO) {
+#endif
         bool display_extension_available = false;
         for (uint32_t i = 0; i < demo->enabled_extension_count; i++) {
             if (strcmp(demo->extension_names[i], VK_KHR_DISPLAY_EXTENSION_NAME) == 0) {
@@ -1436,7 +1436,11 @@ void Demo::init_vk() {
 #endif
 #if defined(VK_USE_PLATFORM_DISPLAY_KHR)
     if (wsi_platform == WsiPlatform::display ||
+#ifdef __ANDROID__
+        wsi_platform == WsiPlatform::display) {
+#else
         wsi_platform == WsiPlatform::auto_) {
+#endif
         bool display_extension_available = false;
         for (auto extension : instance_extensions) {
             if (!strcmp(VK_KHR_DISPLAY_EXTENSION_NAME, extension.extensionName)) {
